/**
 * A pure function that increments a number.
 * @param {number} currentCount - The current count value.
 * @returns {number} The new, incremented count value.
 */
module.exports.increment = (currentCount) => {
    return currentCount + 1;
}